//const { getAllTestResources,uploadTestResource } = require("../api/resource-api.js");

describe('Sanity Test', () => { 
    it('should be true ', () => {
        const sanityTemp = true;
        expect(sanityTemp).toBe(true);
      });
  });
